{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "shortcut": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-shortcut"],
      "enabled": true,
      "environment": {
        "SHORTCUT_API_TOKEN": "{env:SHORTCUT_API_TOKEN}"
      }
    }
  }
}
