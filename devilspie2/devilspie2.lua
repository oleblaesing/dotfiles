if (get_window_name() == "Mozilla Firefox") then
   -- x,y, xsize, ysize
   set_window_geometry2(20, 50, 1880, 1000);
end

if (get_window_name() == "Terminal") then
   -- x,y, xsize, ysize
   set_window_geometry2(20, 50, 1880, 1000);
end
